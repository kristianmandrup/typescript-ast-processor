import {
  testerFor,
  context,
  query,
  logObj,
  log,
} from '../_imports'

describe('function call', () => {
  describe('arrow', () => {
    context('arrow-function file', () => {
      const tester: any = testerFor('arrow-function-call', {
        type: 'function',
        category: 'declaration'
      })
    })
  })
})
